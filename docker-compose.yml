version: '3.9'

# Redis is now handled by Upstash (free cloud Redis 7+)
# Docker only needed if you want a local Redis fallback
# services:
#   redis:
#     image: redis:7-alpine
#     ports:
#       - '6379:6379'